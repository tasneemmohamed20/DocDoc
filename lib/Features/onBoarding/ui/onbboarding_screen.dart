import 'package:flutter/material.dart';

class OnbboardingScreen extends StatelessWidget {
  const OnbboardingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('OnbboardingScreen'),
      ),
    );
  }
}
